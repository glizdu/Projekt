roscore

// trzeba zmienić źródło na source glizdu/devel/setup.bash
// włącza fake_kinecta, symulacje swiata w gazebo wraz z modelem robota
roslaunch glizdu_projekt world_start.launch

// uruchomienie gmapping
roslaunch glizdu_projekt projekt.launch

//wizualizacja
roslaunch glizdu_projekt rviz.launch
